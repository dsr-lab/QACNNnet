import tensorflow as tf

def get_predictions(predictions_start, predictions_end):

    #Reshape (batch_size, n_words)
    #Build product matrix (batch_size, n_words, n_words), first dim start second dim end
    #Create mask
    #Get max from masked matrix
    #Get max's indices
    pass
